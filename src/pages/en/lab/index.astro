---
import ContentListPage from '../../../components/ContentListPage.astro';
import { getPublished, labStatusLabels } from '../../../lib/content';

const description =
  'Experiments in AI-native software businesses—products and business models I build and validate myself.';
const items = (await getPublished('labEn')).sort(
  (a, b) => b.data.startDate.valueOf() - a.data.startDate.valueOf(),
);
---

<ContentListPage
  title="Lab"
  description={description}
  lang="en"
  items={items.map((item) => ({
    href: `/en/lab/${item.id}`,
    title: item.data.title,
    description: item.data.description,
    eyebrow: labStatusLabels[item.data.status],
  }))}
/>
